import {ShieldCheck,Download,BookOpen,LockKeyhole} from 'lucide-react';import {Link} from 'react-router-dom';import AppShell from '../components/AppShell';import MobileNav from '../components/MobileNav';import Card from '../components/Card';import Button from '../components/Button';
const url=import.meta.env.VITE_EXTENSION_URL||'';
export default function Extension(){return <AppShell><div className="page narrow"><div className="page-header"><div><span className="eyebrow">BalRaksha Guardian</span><h1>Extension</h1><p>Protect yourself while using supported online chats.</p></div></div><Card className="extension-card"><div className="extension-logo"><ShieldCheck size={30}/></div><h2>Your safety, with you in control.</h2><p>The browser extension detects risky patterns locally. Conversations stay on your device unless you choose to report.</p><div className="mini-safe"><span/> Local detection enabled in the extension</div><div className="hero-actions"><a href={url||'#'} onClick={e=>{if(!url)e.preventDefault()}} target={url?'_blank':undefined} rel="noopener noreferrer"><Button disabled={!url}><Download size={17}/> {url?'Download Extension':'Download link not configured'}</Button></a><Link to="/extension-guide"><Button variant="secondary"><BookOpen size={17}/> User Guide</Button></Link></div>{!url&&<p className="muted">Add VITE_EXTENSION_URL to your .env file when the extension distribution URL is ready.</p>}<div className="privacy-banner"><LockKeyhole size={20}/><div><strong>Your conversations stay on your device</strong><p>No report → no conversation leaves the device.</p></div></div></Card></div><MobileNav/></AppShell>}
