import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {LifeBuoy,LockKeyhole,ArrowRight,ShieldCheck} from 'lucide-react';import AppShell from '../components/AppShell';import MobileNav from '../components/MobileNav';import Card from '../components/Card';import Button from '../components/Button';import Input from '../components/Input';import {createHelp} from '../services/helpService';
export default function Help(){const nav=useNavigate();const [subject,setSubject]=useState('');const [priority,setPriority]=useState('MEDIUM');const [anonymous,setAnonymous]=useState(true);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const submit=async e=>{e.preventDefault();setBusy(true);try{const r=await createHelp({subject,priority,anonymous});try{localStorage.setItem(`balraksha_help_${r.data.helpRequest.id}`,JSON.stringify(r.data.helpRequest))}catch{} nav(`/help/${r.data.helpRequest.id}`,{state:{request:r.data.helpRequest}})}catch(e){setError(e.response?.data?.message||'Unable to create a help request.')}finally{setBusy(false)}};return <AppShell><div className="page narrow"><div className="help-hero"><div className="help-icon"><LifeBuoy size={30}/></div><span className="eyebrow">Human support</span><h1>Get Help</h1><p>Talk to a trusted coordinator anytime. You’re not alone.</p></div><Card><div className="form-heading"><span className="eyebrow">Start a private conversation</span><h2>What would you like help with?</h2><p>Keep it simple. Share only what feels safe.</p></div>{error&&<div className="alert error">{error}</div>}<form onSubmit={submit}><Input label="What’s going on?" required value={subject} onChange={e=>setSubject(e.target.value)} placeholder="For example: Someone online is making me uncomfortable"/><label className="field"><span>Priority</span><select value={priority} onChange={e=>setPriority(e.target.value)}><option value="LOW">Low</option><option value="MEDIUM">Medium</option><option value="HIGH">High</option><option value="URGENT">Urgent</option></select></label><div className="anonymous-box"><div className="anon-icon"><LockKeyhole size={19}/></div><div><strong>Anonymous by default</strong><p>The current help endpoint does not authenticate this request, so your account is not attached to it. BalRaksha keeps this support request privacy-first.</p></div><span className="badge badge-blue">Private</span></div><Button className="full" disabled={busy}>{busy?'Opening secure chat…':'Ask for help'} <ArrowRight size={18}/></Button></form></Card><div className="support-note"><ShieldCheck size={18}/><span>Support requests are stored by the existing help API. Chat messages are exchanged in real time once a request is opened.</span></div></div><MobileNav/></AppShell>}
