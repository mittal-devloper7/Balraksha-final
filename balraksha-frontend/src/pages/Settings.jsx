import {useAuth} from '../hooks/useAuth';import {ShieldCheck,LockKeyhole,Bell,ScanSearch,LogOut} from 'lucide-react';import AppShell from '../components/AppShell';import MobileNav from '../components/MobileNav';import Card from '../components/Card';import Button from '../components/Button';
export default function Settings(){const {user,logout}=useAuth();return <AppShell><div className="page narrow"><div className="page-header"><div><span className="eyebrow">Your account</span><h1>Settings</h1><p>Simple controls for your BalRaksha experience.</p></div></div><Card className="settings-card"><Setting icon={ShieldCheck} title="Account" text={`${user?.name||'User'} · ${user?.email||''}`}/><Setting icon={LockKeyhole} title="Privacy" text="Reports can be anonymous. Conversations are not uploaded automatically."/><Setting icon={Bell} title="Notifications" text="Notification preferences are handled by the connected product services."/><Setting icon={ScanSearch} title="Extension" text="Guardian detection runs locally in the supported browser extension."/><div className="setting-action"><Button variant="danger" onClick={logout}><LogOut size={17}/> Log out</Button></div></Card></div><MobileNav/></AppShell>}
function Setting({icon:Icon,title,text}){return <div className="setting-row"><div className="setting-icon"><Icon size={20}/></div><div><strong>{title}</strong><span>{text}</span></div></div>}
