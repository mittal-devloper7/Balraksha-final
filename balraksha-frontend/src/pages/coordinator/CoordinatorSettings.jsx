import {User,ShieldCheck,Bell,LockKeyhole} from 'lucide-react';import AppShell from '../../components/AppShell';import Card from '../../components/Card';
export default function CoordinatorSettings(){return <AppShell coordinator><div className="page narrow coordinator-page"><div className="page-header"><div><span className="eyebrow">Coordinator workspace</span><h1>Settings</h1><p>Manage your account and review connected support configuration.</p></div></div><Card className="settings-card">{[[User,'Profile','Your coordinator profile is managed by the connected account.'],[ShieldCheck,'Account','Role-based access is enforced by the backend.'],[Bell,'Notifications','Notification preferences can be connected when backend support is available.'],[LockKeyhole,'Security','Keep your account credentials private and use the approved login flow.']].map(([I,t,d])=><div className="setting-row" key={t}><div className="setting-icon"><I size={20}/></div><div><strong>{t}</strong><span>{d}</span></div></div>)}</Card></div></AppShell>}
