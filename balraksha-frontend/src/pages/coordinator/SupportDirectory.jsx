import {Phone,Mail,Building2,HeartHandshake,ExternalLink} from 'lucide-react';
import Card from '../../components/Card';import Button from '../../components/Button';import {ngoConfig} from '../../config/ngoConfig';
const Action=({href,children,disabled})=><a className={`btn btn-secondary ${disabled?'disabled':''}`} href={disabled?'#':href} onClick={e=>disabled&&e.preventDefault()}>{children}</a>;
export default function SupportDirectory({caseId,summary=''}){const subject=encodeURIComponent(`BalRaksha Case Escalation - ${caseId||'Support Request'}`);const body=encodeURIComponent(`Hello,\n\nI am contacting you regarding a BalRaksha safety case.\nCase ID: ${caseId||'Not provided'}\nSummary: ${summary||'Please review the case in the coordinator dashboard.'}\n\nPlease advise on the appropriate next steps.\n\nRegards,\nBalRaksha Coordinator`);return <Card className="support-directory"><div className="card-heading"><div><span className="eyebrow">External support</span><h2>NGO & Helpline Directory</h2></div><HeartHandshake size={24}/></div><p className="muted">Use only verified contacts configured by your team. No contact is called or emailed automatically.</p><div className="support-grid"><div><Building2 size={20}/><strong>{ngoConfig.name}</strong><span>NGO / child protection partner</span><div className="support-actions">{ngoConfig.email&&<Action href={`mailto:${ngoConfig.email}?subject=${subject}&body=${body}`}><Mail size={15}/> Email NGO</Action>}{ngoConfig.phone&&<Action href={`tel:${ngoConfig.phone}`}><Phone size={15}/> Call NGO</Action>}{ngoConfig.website&&<Action href={ngoConfig.website}><ExternalLink size={15}/> Website</Action>}</div></div><div><Phone size={20}/><strong>{ngoConfig.helplineName}</strong><span>Immediate support helpline</span>{ngoConfig.helplinePhone?<div className="support-actions"><Action href={`tel:${ngoConfig.helplinePhone}`}><Phone size={15}/> Call helpline</Action></div>:<small className="muted">Phone not configured.</small>}</div><div><Building2 size={20}/><strong>{ngoConfig.localAuthorityName}</strong><span>Verified local authority</span>{ngoConfig.localAuthorityPhone?<div className="support-actions"><Action href={`tel:${ngoConfig.localAuthorityPhone}`}><Phone size={15}/> Call authority</Action></div>:<small className="muted">Phone not configured.</small>}</div></div></Card>}
