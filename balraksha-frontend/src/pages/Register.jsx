import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ShieldCheck,ArrowLeft,LockKeyhole} from 'lucide-react';import Input from '../components/Input';import Button from '../components/Button';import {useAuth} from '../hooks/useAuth';
export default function Register(){const {register}=useAuth();const nav=useNavigate();const [form,setForm]=useState({name:'',email:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setError('');setBusy(true);try{await register(form);nav('/dashboard');}catch(err){setError(err.response?.data?.message||'Unable to create your account.');}finally{setBusy(false)}};return <div className="auth-page"><Link to="/" className="back-link"><ArrowLeft size={17}/> Back to BalRaksha</Link><div className="auth-card"><div className="auth-brand"><span><ShieldCheck size={24}/></span><strong>BalRaksha</strong></div><span className="eyebrow">Create your safe space</span><h1>Let’s get you protected.</h1><p>Your account starts as a child account. You can report anonymously by default.</p>{error&&<div className="alert error">{error}</div>}<form onSubmit={submit}><Input label="Your name" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="What should we call you?"/><Input label="Email" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/><Input label="Password" type="password" minLength={8} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 8 characters"/><div className="privacy-check"><LockKeyhole size={18}/><span>We keep reporting privacy-first. Your identity is not included when anonymous reporting is selected.</span></div><Button className="full" disabled={busy}>{busy?'Creating account…':'Create account'}</Button></form><div className="auth-footer">Already have an account? <Link to="/login">Sign in</Link></div></div></div>}
